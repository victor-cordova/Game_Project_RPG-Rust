// use std::io::{stdin};
pub mod get_age;
pub mod calculator;

fn main(){
    // get_age::exercise_get_age();
    calculator::exercise_calculator();
//     let a = 3;
// let b = 1 + 2;
// assert_eq!(a, b);
// // ass
// println!("Corriendo antes del assert!");
// assert!(false, "No se complió la condición");
// assert_eq!(true, false, "Deben ser iguales");
// assert_ne!(true, true, "No deben ser iguales");
// println!("Corriendo despues del assert!");
// assert_eq!(a, b, "we are testing addition with {} and {}", a, b);
}
